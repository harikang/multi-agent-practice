# Streamlit 챗봇 웹앱 실행 가이드

## 🚀 빠른 시작

### 1. Streamlit 설치 확인

```bash
python3 -c "import streamlit; print('Streamlit is installed!')"
```

만약 설치되지 않았다면:

### 2. Streamlit 설치

**방법 1: 가상 환경 사용 (권장)**

```bash
# 가상 환경 생성
python3 -m venv venv

# 가상 환경 활성화
source venv/bin/activate

# 패키지 설치
pip install -r requirements.txt
```

**방법 2: pipx 사용**

```bash
brew install pipx
pipx install streamlit
```

**방법 3: 시스템 전체 설치 (주의)**

```bash
python3 -m pip install streamlit --user
```

### 3. Streamlit 앱 실행

```bash
cd /Users/kang/workshop/multi_agent_practice
streamlit run streamlit_app.py
```

또는 가상 환경 사용 시:

```bash
source venv/bin/activate
streamlit run streamlit_app.py
```

### 4. 브라우저 접속

Streamlit이 자동으로 브라우저를 열거나, 다음 주소로 접속하세요:
```
http://localhost:8501
```

## 🎯 Streamlit 앱 기능

### 주요 기능
- 💬 **채팅 인터페이스**: 자연스러운 대화형 UI
- 📝 **채팅 히스토리**: 세션 동안 대화 내용 유지
- 🎯 **예제 쿼리**: 좌측 사이드바에서 원클릭 실행
- 🎨 **에이전트 뱃지**: 각 에이전트 역할 시각화
- 🔄 **실시간 상태**: 에이전트 처리 상태 표시

### 사이드바 예제 쿼리
1. 📝 **Summarize Paper** - 논문 요약
2. 💻 **Implement Code** - Python 코드 구현
3. 💡 **Generate Ideas** - 후속 연구 아이디어
4. 🔍 **Find Papers** - arXiv 논문 검색
5. 🎯 **Full Analysis** - 종합 분석

## 🛠️ 문제 해결

### Streamlit이 실행되지 않는 경우

1. **포트 충돌 체크**
   ```bash
   lsof -i :8501
   ```

2. **다른 포트로 실행**
   ```bash
   streamlit run streamlit_app.py --server.port 8502
   ```

3. **캐시 삭제**
   ```bash
   streamlit cache clear
   ```

### 에이전트가 응답하지 않는 경우

1. 모든 에이전트 파일이 같은 디렉토리에 있는지 확인
2. strands 및 strands-tools가 설치되어 있는지 확인
3. 터미널에서 에러 메시지 확인

## 💡 사용 팁

- **긴 응답**: 복잡한 쿼리는 처리 시간이 걸릴 수 있습니다
- **히스토리 삭제**: 사이드바의 "Clear Chat History" 버튼 사용
- **예제 활용**: 처음 사용 시 예제 쿼리로 시작하세요
- **구체적 질문**: 명확하고 구체적인 질문이 더 좋은 결과를 제공합니다

## 📸 스크린샷 예시

앱 실행 후 다음과 같은 화면이 표시됩니다:
- 상단: 제목 및 설명
- 좌측: 에이전트 기능 및 예제 쿼리
- 중앙: 채팅 인터페이스
- 하단: 입력 박스

---

## 🔧 고급 설정

### 포트 및 호스트 변경

```bash
streamlit run streamlit_app.py --server.port 8080 --server.address localhost
```

### 디버그 모드

```bash
streamlit run streamlit_app.py --logger.level=debug
```

### 브라우저 자동 열기 비활성화

```bash
streamlit run streamlit_app.py --server.headless true
```

---

**Happy Researching with Streamlit! 🎓✨**
